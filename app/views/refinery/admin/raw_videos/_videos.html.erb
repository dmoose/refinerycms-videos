<%= will_paginate @raw_videos %>
<ul class="<%= ['pagination_frame', pagination_css_class].compact.join(' ') %>">
  <%= render :partial => "video", :collection => @raw_videos %>
</ul>

<script>
  $(document).ready(function() {
    $('.pagination a').attr('data-remote', 'true');
  });
</script>
